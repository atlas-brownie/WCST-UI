const React = require('react');
import { mount } from 'enzyme';
import { AppProviders } from 'app/setupTests';
import { {{pascalCase name}}Page } from '../{{dashCase name}}-page.container';

const componentTree = mount (
    <AppProviders>
        <{{pascalCase name}}Page />
    </AppProviders>
)

describe('{{pascalCase name}}Page', () => {
    it('renders without error', () => {
        expect(componentTree).toBeTruthy();
    });
});