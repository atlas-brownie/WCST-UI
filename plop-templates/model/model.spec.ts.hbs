import { {{pascalCase name}} } from '../{{dashCase name}}';

describe('{{pascalCase name}} Model', () => {
    it('creates without error', () => {
        const modelBase = new {{pascalCase name}}();
        expect(modelBase).toBeTruthy();
    });
});
