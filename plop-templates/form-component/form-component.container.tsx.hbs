import { useState, FunctionComponent, ReactElement } from 'react';
import { {{pascalCase name}}Render } from './{{dashCase name}}.render';
import { ComponentsFormDefinitions } from 'app/models'; // SWITCH THIS MODEL OUT WITH WHATEVER DEFINITIONS MODEL YOU WILL BE USING FOR THE FORM
import { FormikControls } from 'app/shared';
import { useFormik } from 'formik';

export const {{pascalCase name}}: FunctionComponent = (props): ReactElement => {
    function handleSubmit(values: ComponentsFormDefinitions) {
        console.log('{{pascalCase name}} handleSubmit values=', values);
    }

    const Controls = useState(new FormikControls<ComponentsFormDefinitions>(ComponentsFormDefinitions))[0];
    const Render = useState(new {{pascalCase name}}Render())[0];

    Render.props = {
        formControls: useFormik(Controls.buildFormControls(handleSubmit)),
        formDefinitions: Controls.formDefinitions
    };

    return Render.display();
};
