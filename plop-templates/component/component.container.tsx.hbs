import React, { FunctionComponent, ReactElement } from 'react';
import { {{pascalCase name}}Render, {{pascalCase name}}RenderProps } from './{{dashCase name}}.render';

export interface {{pascalCase name}}Props {}

export const {{pascalCase name}}: FunctionComponent<{{pascalCase name}}Props> = (props): ReactElement => {
    const definitions: {{pascalCase name}}RenderProps = {};
    return <{{pascalCase name}}Render {...definitions} />;
};