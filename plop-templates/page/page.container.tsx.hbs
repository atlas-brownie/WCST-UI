import { FunctionComponent, ReactElement, useState } from 'react';
import { {{pascalCase name}}PageRender } from './{{dashCase name}}-page.render';
import { {{pascalCase name}}FormDefinitions } from 'app/models';
import { FormikControls } from 'app/shared';
import { useFormik } from 'formik';

export interface {{pascalCase name}}PageProps {}

export const {{pascalCase name}}Page: FunctionComponent<{{pascalCase name}}PageProps> = (props): ReactElement => {
    function handleSubmit(values: {{pascalCase name}}FormDefinitions) {
        console.log('{{pascalCase name}}Page handleSubmit values=', values);
    }

    const Controls = useState(new FormikControls<{{pascalCase name}}FormDefinitions>({{pascalCase name}}FormDefinitions))[0];
    const formControls = useFormik(Controls.buildFormControls(handleSubmit));

    const Render = useState(new {{pascalCase name}}PageRender())[0];
    Render.props = {
        formControls,
        formDefinitions: Controls.formDefinitions
    };

    return Render.display();
};